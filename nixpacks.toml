# WhatsApp/Puppeteer dependencies removed - they caused build failures due to invalid Nix package names
# If WhatsApp is needed, set ENABLE_WHATSAPP=true and configure Chromium manually
# The core visitor management functionality works without these dependencies

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
